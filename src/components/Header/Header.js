import React from 'react';
import SwiperSlider from '../SwiperSlider/SwiperSlider';

const Header = () => {
  return (
    <div className="p-5">
      <SwiperSlider />
    </div>
  );
};

export default Header;